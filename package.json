{
  "name": "marp-mermaid-engine",
  "version": "0.3.1",
  "private": true,
  "description": "A Marp Core engine that renders Mermaid fences with standard Mermaid.js via Mermaid CLI.",
  "type": "module",
  "exports": "./engine.mjs",
  "files": [
    "engine.mjs",
    "README.md",
    "test/smoke.mjs"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "node test/smoke.mjs"
  },
  "peerDependencies": {
    "@marp-team/marp-core": "^5.0.0",
    "puppeteer": "^23 || ^24 || ^25"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.17.0"
  }
}
